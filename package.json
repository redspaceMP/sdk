{
  "name": "redspacem",
  "private": true,
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "build": "bun run --cwd packages/@redspacem/server-types build && bun run --cwd packages/@redspacem/rpc build && bun run --cwd packages/@redspacem/client-types build && bun run --cwd packages/@redspacem/di build && bun run --cwd packages/@redspacem/browser-types build && bun run --cwd packages/@redspacem/testing build && bun run --cwd resources/freeroam build"
  },
  "type": "module",
  "workspaces": [
    "packages/@redspacem/*",
    "resources/freeroam"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "typescript": "^7.0.2"
  }
}
